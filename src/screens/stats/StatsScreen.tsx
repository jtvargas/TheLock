import React from 'react';

import { StatsScreenProps } from '@type';

import StatsContainer from '@containers/stats';

const StatsScreen: React.FC<StatsScreenProps> = () => {
  return <StatsContainer />;
};

export default StatsScreen;
